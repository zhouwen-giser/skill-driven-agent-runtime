# SDAR v1.3 P13 Release-Candidate Baseline

Status: `FROZEN_FOR_P13_AUDIT`

## Repository state

- Repository: `zhouwen-giser/skill-driven-agent-runtime`
- Candidate branch: `feature/v1.3-sequential-implementation`
- P13 starting commit: `e3d7c8662de78d6a99289183d5345d89adad96cb`
- Frozen v1.2.3 base: `v1.2.3-final` at
  `856f909d22c33e6e20d7e0a1cffc2f54c03b4477`
- `origin/main` observed at P13 bootstrap:
  `940bb96439186acf2151935cc953a51b512abab1`
- P12 verification commit:
  `5cef3a04b7237ac126f7e9d0548347b0d5c25baa`
- P12 completion commit:
  `e3d7c8662de78d6a99289183d5345d89adad96cb`

The worktree was clean at bootstrap and the local branch matched its remote
tracking branch. The candidate was twenty commits ahead and one commit behind
the then-current `origin/main`; its merge base with `origin/main` was
`fe41d60c3c6acc6557ba5a0d702dcb82f767a599`. The repository owner explicitly
instructed P13 to begin from this
already sequentially integrated feature branch and to defer all push and pull
request activity until P14 completes. P13 therefore records the divergence and
does not claim that this starting commit was already merged into `main`.

## Frozen predecessor input

- P00 remains the accepted `READY_FULL` foundation decision.
- P01 through P12 and the mandatory P04R remediation are read-only predecessor
  inputs for P13.
- PostgreSQL remains the system of record. Redis and BullMQ contain only
  rebuildable wake, queue and cache state.
- `UserGoalPlanController`, the formal Workflow/LangGraph runtime, P02 Artifact
  repository, and P06 activation pointer retain their frozen authorities.
- P13 adds audit evidence and narrowly scoped release hardening only. It does
  not add a new product Goal, domain authority or execution runtime.

The machine-readable predecessor integrity, package consistency and authority
results are published separately in:

- `reports/goal/v1.3-final-handoff-integrity.json`
- `reports/goal/v1.3-final-package-consistency.json`
- `reports/goal/v1.3-final-authority-audit.json`

## Toolchain and environment

| Item | Frozen value |
| --- | --- |
| Operating system | Windows x64 |
| Node.js | `v22.14.0` |
| pnpm | `11.7.0` |
| Docker Engine | `29.6.1` |
| Docker Compose | `v5.2.0` |
| Database tests | isolated local Docker PostgreSQL/pgvector databases |
| Queue/cache tests | isolated local Docker Redis databases and unique BullMQ queues |
| Production access | none |

All P13 failure and recovery drills use synthetic non-PII facts. Local Docker
measurements are release-candidate evidence, not production RTO, RPO,
availability or capacity claims.

## Dependency baseline

`pnpm-lock.yaml`, `pnpm-workspace.yaml`, `third_party/sources.lock.yaml`,
`docs/13_OPEN_SOURCE_LICENSE_LEDGER.md` and the container digests form one
audited dependency baseline. P13 may change them only to close a confirmed
Critical/High dependency or container finding, with source intake, an ADR and
regenerated SBOM/license evidence.

## Publication boundary

P13 produces a local release-candidate decision. Per the owner's sequencing
instruction, P13 is committed locally without push. The exact published SHA,
remote branch and `main`-targeted pull request are recorded only after the P14
phase has reached its own truthful terminal state. No merge, tag, release or
production deployment is authorized by this baseline.
