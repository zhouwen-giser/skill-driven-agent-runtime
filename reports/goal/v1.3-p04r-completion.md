# SDAR v1.3 P04R Completion Report

Status: `COMPLETED`

Result: `P04R_COMPLETED`

## Scope

Only P04R was executed. P00-P02 remained frozen/read-only, existing P03/P04 code and V1.1 evidence
were preserved, and P05 implementation was not started.

## Findings Closure

- Activity Identity V1.2 separates lifecycle `eventType` from real `activityKey` and preserves Plan
  Node, Skill Goal/Attempt, Capability, Effect and Provider Operation lineage.
- P03 mining preserves repeated activities, self-loops, parallel, branch and full recovery semantics;
  it computes real quality metrics and emits WorkflowPattern V1.2.
- P04 executes all five anti-overfitting gates, validates the live Capability Catalog, computes three
  independent fingerprints and compiles exact DAG/parallel/conditional/recovery semantics without
  losing parameter, applicability or lineage data.
- Static Validator V1.2 covers Activity Identity, DAG, parallel, criteria, capability shape/catalog,
  parameter policy/schema, applicability, lineage, recovery, replay safety, bounds and duplicates.
- The durable Candidate chain is:
  `P03 WorkflowPattern V1.2 -> PostgreSQL run -> fusion/generalization -> duplicate lookup ->
  candidate/static validation -> P02 ArtifactRepository.saveCandidate -> lineage/validation ->
  Outbox compiler.artifact_candidate_created -> completed run`.
- PostgreSQL/P02 remain authoritative; Redis contains run-ID wakes only.

## Contracts and Bundle

- Shared Interface Registry V1.2:
  `8aa828faf544b2cad3d3eb72bfc0935b02ba324a517de1563308862fc7d60dee`.
- Seven upgraded contracts and their Schema hashes match the Registry and P03/P04/P05 locks.
- Package accounting is 14 formal / 1 mandatory remediation / 1 optional post-release.
- Ordering is `P04 -> P04R -> P05`; P04R creates no G23.
- P05 requires P04R `COMPLETED` and consumes the required V1.2 contracts. This is contract alignment
  only; no P05 replay/validation implementation was added.

## Independent Reviews

| Review | Blocking | Major | Minor | Result |
| --- | ---: | ---: | ---: | --- |
| A — P03 Activity Identity / Mining | 0 | 0 | 3 | Accepted |
| B — P04 Compiler / Candidate Runtime | 0 | 0 | 2 | Accepted |
| C — Registry / P05 / P13 | 0 | 0 | 2 | Accepted |

All earlier Review B Major findings were fixed and independently re-reviewed. Remaining Minors are
recorded in `reports/goal/v1.3-p04r-review.md` and are non-blocking.

## Verification

Exact verified commit: `de25f4c9eb4d9cf96ab75bdaf0d1552a738c3187`.

`reports/verification/summary.json` records `status=passed`, `dirty=false`,
`infrastructureMode=self-managed-compose` and seven passed aggregate stages in 220,389 ms:

- 134 unit/contract files, 848 tests;
- 12 real PostgreSQL/Redis Integration files, 104 tests;
- 2 real PostgreSQL/Redis plus simulated model/MCP E2E files, 62 tests;
- 21 additive migrations through 0128 with rollback/reapply;
- 468 TypeScript sources architecture-checked;
- A2A MUST 74/74, 152 OpenAPI operations, Cognitive Replay with zero physical calls;
- pgvector 0.8.5 / Redis PONG infra smoke and Server/Console smoke.

The individually required commands also passed: format, lint, typecheck, unit, contract, integration,
E2E, migrations, architecture, build and `pnpm verify`.

## Preserved Failures and Corrections

1. Docker access first failed inside the filesystem sandbox; the approved real-infrastructure rerun
   exposed actual P03 regression failures.
2. Skill Goal source refs lacked stable namespacing; the normalizer and real fixture now use the
   Activity identity.
3. The 10k fixture still wrote V1.1 Trace bodies and then lacked an Activity after V1.2 migration;
   it now writes the frozen V1.2 contract and a formal Activity.
4. `verify:migrations` first lacked built `dist`; build was made an explicit prerequisite and the
   migration gate passed.
5. Cognitive Replay used a Windows drive path as an ESM URL; `pathToFileURL` closed the gate.
6. Infra smoke initially targeted protected default `/sdar`; the final run bound every database
   variable to isolated `sdar_p04r_20260728`. The default database was not mutated.

## Commits

- `6996cff` — Activity Identity and process mining.
- `9af979c` — Plan Template compiler semantics.
- `9689a66` — durable Candidate runtime.
- `d5aea4c` — Registry and downstream contract alignment.
- `de25f4c` — implementation-state verification evidence.

P03 and P04 revised Handoffs are `COMPLETED`. P04R Handoff is
`reports/goal/v1.3-p04r-handoff.json`. No push, merge, tag, release or deployment was performed.
