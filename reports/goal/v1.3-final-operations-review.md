# SDAR v1.3 P13 Independent Operations / Release Review

Review mode: independent read-only static review. No verification, migration,
container, recovery, release, deployment or production command was executed in
this phase.

## Blocking

- `OPS-B01` — The exact candidate release gate is not closed. The current
  `reports/verification/summary.json` records commit
  `5cef3a04b7237ac126f7e9d0548347b0d5c25baa` from P12, while
  `reports/goal/v1.3-final-verification-report.json`,
  `reports/goal/v1.3-final-reproducibility-report.json`,
  `reports/goal/v1.3-release-manifest.json`,
  `reports/goal/v1.3-release-candidate-report.md` and the P13 completion/handoff
  are absent. P13 cannot emit `RELEASE_CANDIDATE_READY` until the frozen
  implementation is committed, the complete gate and two clean exact-commit
  builds pass for that same SHA, and the resulting manifest binds those
  reports.

## Major

- `OPS-M01` — The prescribed `pnpm verify` then
  `pnpm verify:v13-reproducibility` sequence cannot currently close.
  `scripts/verify-full.mjs` always rewrites both tracked files
  `reports/verification/summary.json` and
  `reports/verification/summary.md`, but
  `scripts/verify-v13-reproducible-build.mjs` permits only the modified JSON
  path before rejecting the source worktree. A successful full gate will
  therefore leave `summary.md` as a false-positive
  `P13_REPRODUCIBILITY_REQUIRES_CLEAN_SOURCE` failure. The allowlist must cover
  exactly the generated verification evidence pair, while continuing to reject
  every source, lockfile, configuration and unrelated report change.

## Minor

None.

## Accepted

- The migration report exercises the exact frozen v1.2.3 commit
  `856f909d22c33e6e20d7e0a1cffc2f54c03b4477` through an isolated logical
  `pg_dump`/single-transaction `pg_restore` upgrade, preserves representative
  Goal/Plan/Attempt/Outcome/Experience/Tenant/Provider/A2A facts, covers
  interrupted-transaction rollback and checksum-drift rejection, and records
  that existing Compose resources were not touched.
- The hardened PostgreSQL evidence consistently binds migration, recovery and
  Trivy metadata to image ID
  `sha256:856ba6c2ed2292bba994e945ebf1bd638d2c1c78c2562bc9c8b57ea6b9138762`.
  Compose and the shared infrastructure helper disable provenance/SBOM
  attestations for the deterministic local build; the exact PostgreSQL and
  Redis images have zero Critical/High findings in their retained scans.
- Recovery evidence is correctly classified as a real local Docker drill, not
  production proof. It records PostgreSQL authority preservation, Redis
  rebuildability, no duplicate formal or physical side effects, zero
  authoritative facts lost, and local-only measured recovery times.
- Capacity and SLO evidence explicitly sets `productionCapacityClaim=false`
  and `productionSloClaim=false`; no production availability, latency, RTO,
  RPO or cost target is invented.
- Rollout and rollback remain plan-only, default all additive v1.3 paths off,
  require exact tenant and Artifact allowlists, retain cognitive fallback, list
  immediate stop conditions, and require named operations/rollback owners and
  explicit authorization before production activity.
