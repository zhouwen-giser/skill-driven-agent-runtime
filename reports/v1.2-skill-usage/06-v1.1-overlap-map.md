# SDAR v1.2 Phase 6 / v1.1 Overlap and Contract Freeze

- V1.1 final submitted SHA: `9e32311e45a9257741fb7c62f4f89b76dce8360f`
- Integration PR: #4, merged 2026-07-17
- Current `origin/main`: `667146a3639eefdfed9b89c2417c08e1ac50e9a9`
- V1.1-final-in-main ancestry: PASS
- Main-in-v1.2 ancestry: PASS
- Phase 6 branch relationship at entry: 0 behind / 14 ahead
- Merge action: none; branch was created from the integrated baseline

| Shared contract | V1.1 authority retained | V1.2 projection/adaptation rule |
| --- | --- | --- |
| Task operation discovery | `McpTaskOperationCatalog` / `McpTaskOperationSemantics` | resolve Skill Task Type to registered operations; never invent capability |
| Availability and timing | `TaskAvailabilityBatchReader`, request/read-result/check-result, timing/windows/reservation types | Phase 8 adapter directly consumes these types; no cloned enum or resource cache |
| Readiness decision | `DslExecutionReadiness` and `McpTaskReadinessService` | Skill selection summary is advisory; exact-argument pre-invocation check remains mandatory |
| Provider authority | Provider observations, `RemoteTaskBinding` and terminal snapshots | Skill declares demand/preference only; required/forbidden/fallback rules cannot overwrite Provider state |
| External wait | `WorkflowContinuationSnapshot` and PostgreSQL continuation repository | reuse the exact persisted frontier; no Skill checkpoint or second state machine |
| Input/cancel/reconcile | V1.1 remote input, cooperative cancellation, polling/control and restart predicates | reference existing IDs/evidence only; never fabricate Provider acknowledgement or terminal state |
| Workflow planning | existing planner, validator, confirmation and immutable DSL | three IRs become bounded planning input; procedure never executes directly |
| Workflow runtime | existing `compileWorkflow` and LangGraph.js | no second compiler/runtime; only minimal execution-record hooks later |
| Persistence | migrations through `0104_workflow_external_wait_event` | append 0105 usage/import and 0106 execution records; never edit V1.1 migrations |
| API/Console | existing trusted-intranet lifecycle/readiness operations | extend current surfaces; no new management process or authentication model |

Exact readiness projection is frozen as `available → ready`, `restricted → restricted`,
`disabled → unavailable`, and `unknown/errors → unknown`. Phase 8 must retain `validUntil`, earliest and
multiple windows, reservation mode/reference, risk and possible effects in candidate evidence. A
guaranteed result without a valid reservation reference fails closed. Phase 4 mocks are not production
interoperability evidence.
