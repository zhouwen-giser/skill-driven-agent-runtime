# Third-party management

`sources.lock.yaml` is a planning lockfile. EP-00 must replace every `UNPINNED` and `VERIFY_AT_PIN` with exact evidence before release. Research clones are not committed; production dependencies use package lockfiles. Copied code, if any, lives under `vendor/` with source manifests and preserved notices.
