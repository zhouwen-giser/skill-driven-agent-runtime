version: 1.0.0
delivery: at_least_once
events:
- eventType: node.profile.changed
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.health.changed
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.configuration.revision_published
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.configuration.revision_applied
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.configuration.revision_rejected
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.llm.provider_changed
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.smpp.source_changed
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.mcp.provider_binding_changed
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.skill.version_changed
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.plan_template.version_changed
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.capability.version_published
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.capability.version_suspended
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.capability.version_deprecated
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.capability.version_retired
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.capability.readiness_changed
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.a2a.exposure_changed
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.agent_card.activated
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.task.capability_bound
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.management_operation.completed
  authority: hint_only
  consumerAction: refetch_resource
- eventType: node.telemetry_export.status_changed
  authority: hint_only
  consumerAction: refetch_resource
