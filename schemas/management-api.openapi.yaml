openapi: 3.1.0
info:
  title: Skill-Driven Agent Runtime Management API
  version: 1.0.0-dev
  description: Unauthenticated V1 API for trusted-intranet deployments only.
servers:
  - url: http://127.0.0.1:9998
paths:
  /api/v1/memories:
    post:
      operationId: createMemory
      description: Creates one globally shared, source-linked memory and persists its pgvector embedding.
      responses:
        '201': { description: Active global memory item }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/memories/search:
    get:
      operationId: searchMemories
      parameters:
        - { name: q, in: query, required: true, schema: { type: string } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 100 } }
      responses:
        '200': { description: Global active memories ordered by pgvector similarity }
  /api/v1/memories/{memoryId}:
    parameters: [{ name: memoryId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: getMemory
      responses:
        '200': { description: One source-traceable memory item }
        '404': { $ref: '#/components/responses/Error' }
  /api/v1/contexts/{contextId}/goals:
    parameters: [{ name: contextId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: listContextGoalHistory
      description: Returns ordered Goals and fixed-stage relationship decisions for one context.
      responses:
        '200': { description: Goal history and related-successor/unrelated-new transition evidence }
  /api/v1/system/task-wait-policy:
    get:
      operationId: getTaskWaitPolicy
      responses:
        '200': { description: PostgreSQL-authoritative unified confirmation/input wait timeout }
    put:
      operationId: updateTaskWaitPolicy
      description: Changes the timeout applied to all current and future confirmation/input waits.
      responses:
        '200': { description: Updated wait policy }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/goals:
    post:
      operationId: createGoal
      responses:
        '201': { description: PostgreSQL-authoritative active Goal }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/goals/{goalId}:
    parameters: [{ name: goalId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: getGoal
      responses:
        '200': { description: Current Goal record }
        '404': { $ref: '#/components/responses/Error' }
  /api/v1/goals/{goalId}/cancel:
    parameters: [{ name: goalId, in: path, required: true, schema: { type: string } }]
    post:
      operationId: cancelGoal
      description: Applies active Workflow cancellation policies before atomically terminating all remaining Goal work.
      responses:
        '201': { description: Goal cancellation cascade and policy warnings }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/goals/{goalId}/cancellations:
    parameters: [{ name: goalId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: listGoalCancellations
      responses:
        '200': { description: Immutable Goal cancellation history }
  /api/v1/goal-cancellations/{cancellationId}:
    parameters: [{ name: cancellationId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: getGoalCancellation
      responses:
        '200': { description: One Goal cancellation record }
        '404': { $ref: '#/components/responses/Error' }
  /api/v1/goals/{goalId}/patches:
    parameters: [{ name: goalId, in: path, required: true, schema: { type: string } }]
    post:
      operationId: applyGoalPatch
      description: Applies a structured Goal version change, atomically invalidates all old-version execution state, and generates a fresh-confirmation plan.
      responses:
        '201': { description: Persisted Goal Patch evidence and new plan identity }
        '400': { $ref: '#/components/responses/Error' }
    get:
      operationId: listGoalPatches
      responses:
        '200': { description: Ordered immutable Goal Patch history }
  /api/v1/goal-patches/{patchId}:
    parameters: [{ name: patchId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: getGoalPatch
      responses:
        '200':
          { description: Goal Patch before/after, invalidation, compensation, and plan evidence }
        '404': { $ref: '#/components/responses/Error' }
  /api/v1/tasks/{taskId}:
    parameters: [{ name: taskId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: getTask
      responses:
        '200': { description: PostgreSQL-authoritative Task including its current plan binding }
        '404': { $ref: '#/components/responses/Error' }
  /api/v1/tasks/{taskId}/events:
    parameters: [{ name: taskId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: listTaskRuntimeEvents
      responses:
        '200': { description: Ordered displayable Task lifecycle events }
  /api/v1/tasks/{taskId}/processed-results:
    parameters: [{ name: taskId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: listTaskProcessedResults
      responses:
        '200': { description: Normalized results, facts, value assessments and memory candidates }
  /api/v1/tasks/{taskId}/input-inferences:
    parameters: [{ name: taskId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: listTaskInputInferences
      responses:
        '200': { description: Ordered missing-input decisions and explainable evidence sources }
  /api/v1/processed-results/{resultId}:
    parameters: [{ name: resultId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: getProcessedResult
      responses:
        '200': { description: One immutable processed-result record }
        '404': { $ref: '#/components/responses/Error' }
  /api/v1/tasks/{taskId}/plan:
    parameters: [{ name: taskId, in: path, required: true, schema: { type: string } }]
    put:
      operationId: attachTaskPlan
      description: Binds a real persisted plan to a Task at the planning/confirmation boundary.
      responses:
        '200': { description: Task with Goal and plan identity }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/tasks/{taskId}/actions:
    parameters: [{ name: taskId, in: path, required: true, schema: { type: string } }]
    post:
      operationId: actOnTaskPlan
      description: Uses the same TaskService path as A2A follow-up messages to confirm, reject, or naturally revise a plan.
      responses:
        '200': { description: Authoritative Task state after the action }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/workflow-controls:
    post:
      operationId: startWorkflowControl
      description: Executes confirmed immutable plans and requires a structured Goal evaluation after every round. An optional taskId projects capability-gap evidence onto the authoritative A2A Task. Plan adjustment, Skill replacement, and additional-Skill decisions create new versions outside LangGraph; input and capability-gap decisions stop without starting another node.
      responses:
        '201':
          {
            description: Terminal,
            awaiting-confirmation,
            awaiting-input,
            or capability-gap Workflow control,
          }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/workflow-controls/{controlId}:
    parameters: [{ name: controlId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: getWorkflowControl
      responses:
        '200': { description: Persisted outer-control state }
        '404': { $ref: '#/components/responses/Error' }
  /api/v1/workflow-controls/{controlId}/rounds:
    parameters: [{ name: controlId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: listWorkflowControlRounds
      responses:
        '200': { description: Ordered plan, instance, version and structured evaluation evidence }
  /api/v1/workflow-controls/{controlId}/continue:
    parameters: [{ name: controlId, in: path, required: true, schema: { type: string } }]
    post:
      operationId: continueWorkflowControl
      responses:
        '200': { description: Continued control after current plan confirmation }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/workflows/validate:
    post:
      operationId: validateWorkflowDsl
      responses:
        '200': { description: Validated WorkflowDefinition }
        '422': { description: Structured errors; workflow is not executable }
  /api/v1/workflows/plan:
    post:
      operationId: planWorkflow
      responses:
        '201': { description: Immutable validated Workflow plan and confirmation status }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/workflows/plans/{planId}/confirm:
    parameters: [{ name: planId, in: path, required: true, schema: { type: string } }]
    post:
      operationId: confirmWorkflowPlan
      responses:
        '200': { description: Confirmed immutable Workflow plan }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/workflows/plans/{planId}/execute:
    parameters: [{ name: planId, in: path, required: true, schema: { type: string } }]
    post:
      operationId: executeConfirmedWorkflowPlan
      description: Optional skillIds resolve current enabled Skill budget overrides; the immutable resolved limits and usage are returned on the Workflow instance.
      responses:
        '201': { description: Persisted terminal Workflow instance }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/workflows/plans/{planId}/pause:
    parameters: [{ name: planId, in: path, required: true, schema: { type: string } }]
    post:
      operationId: pauseWorkflowExecution
      description: Requests an in-process LangGraph interrupt after the active node and before any subsequent node starts.
      responses:
        '200': { description: Persisted Task-pause checkpoint and boundary identity }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/workflows/plans/{planId}/resume:
    parameters: [{ name: planId, in: path, required: true, schema: { type: string } }]
    post:
      operationId: resumeWorkflowExecution
      description: Resumes the same checkpoint below the Skill threshold; otherwise reports that replanning and fresh confirmation are required.
      responses:
        '200': { description: Resume disposition and Workflow instance }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/workflows/plans/{planId}/cancel:
    parameters: [{ name: planId, in: path, required: true, schema: { type: string } }]
    post:
      operationId: cancelWorkflowExecution
      description: Applies the resolved Skill cancellation strategy, prevents subsequent nodes, and never performs automatic compensation.
      responses:
        '200': { description: Persisted canceled Workflow instance and policy audit }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/workflows/instances/{instanceId}/human-confirmation:
    parameters: [{ name: instanceId, in: path, required: true, schema: { type: string } }]
    post:
      operationId: resumeWorkflowHumanConfirmation
      description: Resumes the same in-process LangGraph checkpoint. A missing checkpoint fails explicitly and is never replayed.
      responses:
        '200': { description: Resumed, paused-at-next-gate, or terminal Workflow instance }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/workflows/instances/{instanceId}:
    parameters: [{ name: instanceId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: getWorkflowInstanceTrace
      description: Returns the immutable instance snapshot and ordered displayable node-event summaries.
      responses:
        '200': { description: Workflow instance and ordered node events for trace and replay }
        '404': { $ref: '#/components/responses/Error' }
  /api/v1/workflows/plans/{planId}:
    parameters: [{ name: planId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: getWorkflowPlan
      responses:
        '200': { description: Immutable plan, revision lineage and confirmation status }
        '404': { $ref: '#/components/responses/Error' }
  /api/v1/workflows/plans/{planId}/trace:
    parameters: [{ name: planId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: getLatestWorkflowTraceForPlan
      responses:
        '200': { description: Latest immutable instance and ordered node events for the Plan }
        '404': { $ref: '#/components/responses/Error' }
  /api/v1/workflows/plans/{planId}/revisions:
    parameters: [{ name: planId, in: path, required: true, schema: { type: string } }]
    post:
      operationId: reviseWorkflowPlanAsAdministrator
      description: Validates canonical DSL or DAG-editor serialization, atomically supersedes the source plan, and always returns the revision to awaiting confirmation.
      responses:
        '201': { description: Validated immutable revision with source lineage }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/prompts:
    post:
      operationId: createPromptVersion
      responses:
        {
          '201': { description: Candidate or enabled immutable PromptVersion },
          '400': { $ref: '#/components/responses/Error' },
        }
  /api/v1/prompts/{promptId}/versions:
    get:
      operationId: listPromptVersions
      responses: { '200': { description: Immutable Prompt version chain } }
  /api/v1/prompts/{promptId}/publish/{version}:
    post:
      operationId: publishPromptCandidate
      responses: { '200': { description: New enabled PromptVersion } }
  /api/v1/prompts/{promptId}/disable:
    post:
      operationId: disablePrompt
      responses: { '200': { description: New disabled PromptVersion } }
  /api/v1/prompts/{promptId}/rollback/{version}:
    post:
      operationId: rollbackPrompt
      responses: { '200': { description: New enabled rollback PromptVersion } }
  /api/v1/prompts/{promptId}/effects/{version}:
    get:
      operationId: getPromptEffect
      responses: { '200': { description: Invocation success/failure/duration/Token summary } }
  /api/v1/models/providers/{providerId}:
    parameters: [{ name: providerId, in: path, required: true, schema: { type: string } }]
    put:
      operationId: configureModelProvider
      description: Stores an AES-GCM encrypted credential and protocol-neutral Provider configuration. Provider kind is independent from the explicit HTTP API style.
      requestBody:
        {
          required: true,
          content:
            {
              application/json:
                {
                  schema:
                    {
                      type: object,
                      additionalProperties: false,
                      required:
                        [
                          name,
                          kind,
                          apiStyle,
                          baseUrl,
                          model,
                          enabled,
                          timeoutMs,
                          credentialHeaders,
                        ],
                      properties:
                        {
                          name: { type: string },
                          kind: { enum: [openai_compatible, local, other_vendor] },
                          apiStyle: { enum: [openai_chat_completions, anthropic_messages] },
                          baseUrl: { type: string, format: uri },
                          model: { type: string },
                          enabled: { type: boolean },
                          timeoutMs: { type: integer, minimum: 1 },
                          credentialHeaders:
                            { type: object, additionalProperties: { type: string } },
                        },
                    },
                },
            },
        }
      responses:
        { '204': { description: Configured }, '400': { $ref: '#/components/responses/Error' } }
  /api/v1/models/routes/{stage}:
    parameters: [{ name: stage, in: path, required: true, schema: { type: string } }]
    put:
      operationId: routeModelStage
      requestBody:
        {
          required: true,
          content:
            {
              application/json:
                {
                  schema:
                    {
                      type: object,
                      required: [providerId],
                      properties: { providerId: { type: string } },
                    },
                },
            },
        }
      responses:
        {
          '204': { description: Fixed route configured },
          '400': { $ref: '#/components/responses/Error' },
        }
  /api/v1/models/invocations:
    get:
      operationId: listModelInvocations
      parameters:
        - { name: stage, in: query, required: false, schema: { type: string } }
        - { name: taskId, in: query, required: false, schema: { type: string, minLength: 1 } }
      responses: { '200': { description: Sanitized model invocation audits } }
  /api/v1/health:
    get:
      operationId: getHealth
      responses:
        '200': { description: Health and deployment risk marker }
  /api/v1/mcp/invocations:
    get:
      operationId: listTaskMcpInvocations
      parameters:
        - { name: taskId, in: query, required: true, schema: { type: string, minLength: 1 } }
      responses:
        '200': { description: Sanitized MCP invocation audits linked to the Task }
  /api/v1/mcp/servers:
    get:
      operationId: listMcpServers
      responses:
        '200': { description: Credential-free MCP Server list }
    post:
      operationId: registerMcpServer
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RegisterMcpServer' }
      responses:
        '201': { description: Registered Server and discovered Tools }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/mcp/servers/{serverId}:
    parameters: [{ $ref: '#/components/parameters/ServerId' }]
    delete:
      operationId: deleteMcpServer
      responses:
        '204': { description: Deleted }
  /api/v1/mcp/servers/{serverId}/refresh:
    parameters: [{ $ref: '#/components/parameters/ServerId' }]
    post:
      operationId: refreshMcpServer
      responses:
        '200': { description: Refreshed Tools and dependency changes }
  /api/v1/mcp/servers/{serverId}/health:
    parameters: [{ $ref: '#/components/parameters/ServerId' }]
    post:
      operationId: checkMcpServerHealth
      responses:
        '200': { description: Remote protocol health, status, and duration }
  /api/v1/mcp/servers/{serverId}/credentials:
    parameters: [{ $ref: '#/components/parameters/ServerId' }]
    put:
      operationId: updateMcpServerCredentials
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [credentialHeaders]
              properties:
                credentialHeaders: { type: object, additionalProperties: { type: string } }
      responses:
        '204': { description: Remotely validated and encrypted credentials updated }
  /api/v1/mcp/servers/{serverId}/tools:
    parameters: [{ $ref: '#/components/parameters/ServerId' }]
    get:
      operationId: listMcpTools
      responses:
        '200': { description: Current original schemas and enhancements }
  /api/v1/mcp/servers/{serverId}/tools/{toolName}/enhancement:
    parameters:
      - $ref: '#/components/parameters/ServerId'
      - { name: toolName, in: path, required: true, schema: { type: string } }
    put:
      operationId: updateMcpToolEnhancement
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ToolEnhancement' }
      responses:
        '204': { description: Updated }
  /api/v1/mcp/servers/{serverId}/invocations:
    parameters: [{ $ref: '#/components/parameters/ServerId' }]
    get:
      operationId: listMcpInvocations
      responses:
        '200': { description: Replayable invocation summaries }
  /api/v1/mcp/servers/{serverId}/operations:
    parameters: [{ $ref: '#/components/parameters/ServerId' }]
    get:
      operationId: listMcpManagementOperations
      description: Returns credential-safe, append-only administrator operation evidence.
      responses:
        '200':
          {
            description: MCP registration,
            refresh,
            health,
            credential,
            metadata,
            and deletion operations,
          }
  /api/v1/mcp/servers/{serverId}/warnings:
    parameters: [{ $ref: '#/components/parameters/ServerId' }]
    get:
      operationId: listMcpDependencyWarnings
      responses:
        '200': { description: Skill dependency warnings }
  /api/v1/skills:
    get:
      operationId: listCurrentSkillVersions
      responses:
        '200': { description: Current Skill versions }
    post:
      operationId: registerSkillVersion
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: true }
      responses:
        '201': { description: New current Skill version }
  /api/v1/skills/author:
    post:
      operationId: authorAndRegisterSkill
      description: Uses the configured structured ModelProvider; never falls back to a placeholder Schema.
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: true }
      responses:
        '201': { description: Model-authored, validated, persisted SkillVersion }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/skill-selections:
    post:
      operationId: selectSkill
      description: Builds pgvector and operational-metric candidates, then requires a configured final decider.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [goalDescription]
              properties:
                goalDescription: { type: string, minLength: 1 }
      responses:
        '201':
          {
            description: Persisted selection,
            candidate metric snapshots,
            and displayable decision summary,
          }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/skill-graph:
    get:
      operationId: listSkillGraph
      responses:
        '200': { description: All typed Skill relation edges }
  /api/v1/skill-graph/relations:
    post:
      operationId: createSkillRelation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sourceSkillId, targetSkillId, relationType]
              properties:
                sourceSkillId: { type: string }
                targetSkillId: { type: string }
                relationType:
                  enum:
                    [
                      parent_child,
                      depends_on,
                      input_output_match,
                      alternative,
                      composition,
                      capability_coverage,
                    ]
                metadata: { type: object, additionalProperties: true }
      responses:
        '201': { description: Created graph edge }
  /api/v1/skill-graph/relations/{relationId}:
    parameters:
      - { name: relationId, in: path, required: true, schema: { type: string } }
    delete:
      operationId: deleteSkillRelation
      responses:
        '204': { description: Deleted graph edge }
  /api/v1/skills/{skillId}/enable:
    parameters: [{ $ref: '#/components/parameters/SkillId' }]
    post:
      operationId: enableSkill
      responses:
        '200': { description: New enabled Skill version }
  /api/v1/skills/{skillId}/disable:
    parameters: [{ $ref: '#/components/parameters/SkillId' }]
    post:
      operationId: disableSkill
      responses:
        '200': { description: New disabled Skill version }
  /api/v1/skills/{skillId}/versions:
    parameters: [{ $ref: '#/components/parameters/SkillId' }]
    get:
      operationId: listSkillVersions
      responses:
        '200': { description: Immutable Skill version chain }
  /api/v1/skills/{skillId}/diff:
    parameters:
      - $ref: '#/components/parameters/SkillId'
      - { name: from, in: query, required: true, schema: { type: integer, minimum: 1 } }
      - { name: to, in: query, required: true, schema: { type: integer, minimum: 1 } }
    get:
      operationId: diffSkillVersions
      responses:
        '200': { description: Changed fields and both immutable versions }
  /api/v1/skills/{skillId}/rollback/{version}:
    parameters:
      - $ref: '#/components/parameters/SkillId'
      - { name: version, in: path, required: true, schema: { type: integer, minimum: 1 } }
    post:
      operationId: rollbackSkill
      responses:
        '200': { description: New current version copied from the target }
  /api/v1/tasks/{taskId}/temporary-skills:
    parameters:
      - { name: taskId, in: path, required: true, schema: { type: string } }
    get:
      operationId: listTaskTemporarySkills
      responses:
        '200': { description: Task-scoped Temporary Skills, including expired records }
    post:
      operationId: createTaskTemporarySkill
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateTemporarySkill' }
      responses:
        '201': { description: Active task-scoped Temporary Skill }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/temporary-skills/{temporarySkillId}/complete:
    parameters:
      - { name: temporarySkillId, in: path, required: true, schema: { type: string } }
    post:
      operationId: completeTemporarySkill
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [successful, outcomeSummary]
              properties:
                successful: { type: boolean }
                outcomeSummary: { type: string, minLength: 1 }
      responses:
        '200':
          { description: Expired Skill, Experience, and optional awaiting-simulation candidate }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/memories/{memoryId}/supersede:
    parameters: [{ $ref: '#/components/parameters/MemoryId' }]
    post:
      operationId: supersedeMemory
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SupersedeMemory' }
      responses:
        '201': { description: New active Memory and immutable supersede transition }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/memories/{memoryId}/invalidate:
    parameters: [{ $ref: '#/components/parameters/MemoryId' }]
    post:
      operationId: invalidateMemory
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/MemoryStatusAction' }
      responses:
        '200': { description: Invalidated Memory and immutable status transition }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/memories/{memoryId}/transitions:
    parameters: [{ $ref: '#/components/parameters/MemoryId' }]
    get:
      operationId: listMemoryTransitions
      responses:
        '200': { description: Append-only Memory status history }
  /api/v1/system/memory-retention-policy:
    get:
      operationId: getMemoryRetentionPolicy
      responses:
        '200': { description: V1 retention controls and disabled automatic-cleanup flags }
    put:
      operationId: updateMemoryRetentionPolicy
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/MemoryRetentionPolicy' }
      responses:
        '200': { description: Updated retention controls }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/system/evolution-policy:
    get:
      operationId: getEvolutionPolicy
      responses:
        '200': { description: Current successful-experience threshold }
    put:
      operationId: updateEvolutionPolicy
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [successThreshold]
              properties: { successThreshold: { type: integer, minimum: 2 } }
      responses:
        '200': { description: Updated evolution threshold }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/evolution-triggers:
    get:
      operationId: listEvolutionTriggers
      parameters:
        - { name: capabilityFingerprint, in: query, schema: { type: string, minLength: 1 } }
      responses:
        '200': { description: Audited evolution threshold decisions }
  /api/v1/tasks/{taskId}/quality-report:
    parameters: [{ $ref: '#/components/parameters/TaskId' }]
    get:
      operationId: getTaskQualityReport
      responses:
        '200': { description: Five-component immutable Task quality report }
        '404': { $ref: '#/components/responses/Error' }
  /api/v1/tasks/{taskId}/implicit-feedback:
    parameters: [{ $ref: '#/components/parameters/TaskId' }]
    get:
      operationId: listTaskImplicitFeedback
      responses:
        '200': { description: Low-confidence behavior-derived feedback observations }
  /api/v1/task-quality-reports/{reportId}/influence:
    parameters: [{ name: reportId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: getTaskQualityReportInfluence
      responses:
        '200': { description: Skill, Workflow-template, and Prompt effects linked to the report }
  /api/v1/skill-formalization-candidates/{candidateId}:
    parameters: [{ $ref: '#/components/parameters/CandidateId' }]
    get:
      operationId: getSkillFormalizationCandidate
      responses:
        '200': { description: Candidate, simulations, publication, and correction history }
        '404': { $ref: '#/components/responses/Error' }
  /api/v1/skill-formalization-candidates/{candidateId}/simulate:
    parameters: [{ $ref: '#/components/parameters/CandidateId' }]
    post:
      operationId: simulateSkillFormalizationCandidate
      responses:
        '200': { description: Five-part simulation report and publication decision }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/skill-formalization-candidates/{candidateId}/corrections:
    parameters: [{ $ref: '#/components/parameters/CandidateId' }]
    get:
      operationId: listSkillFormalizationCorrections
      responses:
        '200': { description: Immutable manual correction Experience history }
    post:
      operationId: correctSkillFormalizationCandidate
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CorrectEvolutionCandidate' }
      responses:
        '200': { description: Corrected candidate and full revalidation result }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/evolution-experiences/{experienceId}:
    parameters: [{ name: experienceId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: getEvolutionExperience
      responses:
        '200': { description: Source-linked evolution Experience }
        '404': { $ref: '#/components/responses/Error' }
  /api/v1/goals/{goalId}/evolution-experiences:
    parameters: [{ name: goalId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: listGoalEvolutionExperiences
      responses:
        '200': { description: Evolution Experiences linked to a Goal }
  /api/v1/skills/{skillId}/evolution-experiences:
    parameters: [{ $ref: '#/components/parameters/SkillId' }]
    get:
      operationId: listSkillEvolutionExperiences
      responses:
        '200': { description: Evolution Experiences linked to a Skill }
  /api/v1/skill-drafts/{draftId}:
    parameters: [{ name: draftId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: getSkillDraft
      responses:
        '200': { description: Generated Skill draft and validation evidence }
        '404': { $ref: '#/components/responses/Error' }
  /api/v1/skill-drafts/{draftId}/publish:
    parameters: [{ name: draftId, in: path, required: true, schema: { type: string } }]
    post:
      operationId: publishSkillDraft
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [actor, skillId, toolPolicy, runtimePolicy, status]
              properties:
                actor: { type: string, minLength: 1 }
                skillId: { type: string, minLength: 1 }
                toolPolicy: { type: object, additionalProperties: true }
                runtimePolicy: { type: object, additionalProperties: true }
                status: { enum: [enabled, disabled] }
      responses:
        '201': { description: Published formal Skill version }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/skills/{skillId}/quality-observations:
    parameters: [{ $ref: '#/components/parameters/SkillId' }]
    post:
      operationId: recordSkillQualityObservation
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SkillQualityObservation' }
      responses:
        '201': { description: Persisted quality observation and optional warning }
        '400': { $ref: '#/components/responses/Error' }
  /api/v1/skill-quality-warnings:
    get:
      operationId: listSkillQualityWarnings
      parameters:
        - { name: skillId, in: query, schema: { type: string, minLength: 1 } }
      responses:
        '200': { description: Warning-only low-quality Skill signals }
  /api/v1/workflow-templates:
    get:
      operationId: listWorkflowTemplates
      parameters:
        - { name: skillId, in: query, schema: { type: string, minLength: 1 } }
        - { name: capabilityFingerprint, in: query, schema: { type: string, minLength: 1 } }
      responses:
        '200': { description: Versioned successful Workflow templates }
  /api/v1/workflow-templates/{templateId}/uses:
    parameters: [{ name: templateId, in: path, required: true, schema: { type: string } }]
    get:
      operationId: listWorkflowTemplateUses
      responses:
        '200': { description: Immutable template reuse and effect evidence }
  /api/v1/evaluation/analytics:
    get:
      operationId: getEvaluationAnalytics
      description: Aggregates immutable execution, quality, model, and Tool evidence from PostgreSQL.
      parameters:
        - { name: skillId, in: query, schema: { type: string, minLength: 1 } }
        - { name: skillVersion, in: query, schema: { type: integer, minimum: 1 } }
        - { name: providerId, in: query, schema: { type: string, minLength: 1 } }
        - { name: model, in: query, schema: { type: string, minLength: 1 } }
        - { name: serverId, in: query, schema: { type: string, minLength: 1 } }
        - { name: toolName, in: query, schema: { type: string, minLength: 1 } }
      responses:
        '200':
          description: Success, duration, cost, failure, version-stability, and quality-trend metrics.
          content:
            application/json:
              example:
                filters: { skillId: device-status, skillVersion: 2 }
                sampleCount: 3
                successCount: 2
                successRate: 0.666667
                averageDurationMs: 125
                totalCost: 3
                averageCost: 1
                failureTypes: [{ code: MCP_TIMEOUT, count: 1 }]
                versionStability:
                  - {
                      skillId: device-status,
                      skillVersion: 2,
                      sampleCount: 3,
                      successRate: 0.666667,
                      averageQuality: 0.8,
                      qualityDeviation: 0.1,
                      stabilityScore: 0.6,
                    }
                qualityTrend: []
        '400': { $ref: '#/components/responses/Error' }
components:
  parameters:
    CandidateId: { name: candidateId, in: path, required: true, schema: { type: string } }
    MemoryId: { name: memoryId, in: path, required: true, schema: { type: string } }
    ServerId: { name: serverId, in: path, required: true, schema: { type: string } }
    SkillId: { name: skillId, in: path, required: true, schema: { type: string } }
    TaskId: { name: taskId, in: path, required: true, schema: { type: string } }
  responses:
    Error:
      description: Stable error envelope
      content:
        application/json:
          schema:
            type: object
            required: [error]
            properties:
              error:
                type: object
                required: [code, message]
                properties:
                  code: { type: string }
                  message: { type: string }
  schemas:
    MemoryRetentionPolicy:
      type: object
      additionalProperties: false
      required:
        [
          reviewAfterDays,
          archiveAfterDays,
          deleteAfterDays,
          automaticArchiveEnabled,
          automaticDeleteEnabled,
        ]
      properties:
        reviewAfterDays: { type: integer, minimum: 1 }
        archiveAfterDays: { type: [integer, 'null'], minimum: 1 }
        deleteAfterDays: { type: [integer, 'null'], minimum: 1 }
        automaticArchiveEnabled: { type: boolean, const: false }
        automaticDeleteEnabled: { type: boolean, const: false }
    MemoryStatusAction:
      type: object
      additionalProperties: false
      required: [actor, reason]
      properties:
        actor: { type: string, minLength: 1 }
        reason: { type: string, minLength: 1 }
    SupersedeMemory:
      allOf:
        - $ref: '#/components/schemas/MemoryStatusAction'
        - type: object
          required: [type, content, summary, sourceRefs, confidence]
          properties:
            type:
              {
                enum:
                  [
                    fact,
                    success_experience,
                    failure_experience,
                    workflow_pattern,
                    skill_learning,
                    prompt_learning,
                  ],
              }
            content: { type: object, additionalProperties: true }
            summary: { type: string, minLength: 1 }
            sourceRefs: { type: array, minItems: 1, items: { type: string, minLength: 1 } }
            confidence: { type: number, minimum: 0, maximum: 1 }
    CorrectEvolutionCandidate:
      type: object
      additionalProperties: false
      required: [actor, summary, proposedSkill]
      properties:
        actor: { type: string, minLength: 1 }
        summary: { type: string, minLength: 1 }
        proposedSkill: { type: object, additionalProperties: true }
    SkillQualityObservation:
      type: object
      additionalProperties: false
      required: [skillVersion, evaluationRef, score, successful]
      properties:
        skillVersion: { type: integer, minimum: 1 }
        evaluationRef: { type: string, minLength: 1 }
        score: { type: number, minimum: 0, maximum: 1 }
        successful: { type: boolean }
    RegisterMcpServer:
      type: object
      additionalProperties: false
      required: [serverId, name, endpoint, credentialHeaders]
      properties:
        serverId: { type: string, minLength: 1 }
        name: { type: string, minLength: 1 }
        endpoint: { type: string, format: uri }
        credentialHeaders: { type: object, additionalProperties: { type: string } }
    ToolEnhancement:
      type: object
      additionalProperties: false
      required: [purpose, scenarios, constraints, returnDescription, commonErrors, tags]
      properties:
        purpose: { type: string }
        scenarios: { type: array, items: { type: string } }
        constraints: { type: array, items: { type: string } }
        returnDescription: { type: string }
        commonErrors: { type: array, items: { type: string } }
        tags: { type: array, items: { type: string } }
    CreateTemporarySkill:
      type: object
      additionalProperties: false
      required: [contextId, name, description, tools, inputSchema, outputSchema]
      properties:
        contextId: { type: string, minLength: 1 }
        name: { type: string, minLength: 1 }
        description: { type: string, minLength: 1 }
        tools:
          type: array
          minItems: 1
          items:
            type: object
            additionalProperties: false
            required: [serverId, toolName]
            properties:
              serverId: { type: string, minLength: 1 }
              toolName: { type: string, minLength: 1 }
        inputSchema: { oneOf: [{ type: boolean }, { type: object }] }
        outputSchema: { oneOf: [{ type: boolean }, { type: object }] }
