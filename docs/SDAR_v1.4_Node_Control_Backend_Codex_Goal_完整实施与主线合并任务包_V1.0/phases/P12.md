# P12 — Organization-facing Profile 与 Node Events

## 建议实施提交

```text
feat(v1.4-p12): expose organization node profile
```

证据提交：

```text
docs(v1.4-p12): record completion evidence
```

## 工作
1. 实现最小 Node Profile/Version/Health。
2. Capability Catalog/Readiness、A2A Exposure、Configuration Summary。
3. ManagementOperation Query 和 Node Event Stream。
4. Events 只提示变化，支持 Event ID/Revision/Correlation。
5. 组织 Client RBAC 与稳定 API Profile。
6. 不实现组织树、多节点编排或 Console BFF。

## 验收
- Profile 不暴露 Runtime DB/内部 LangGraph。
- Event 乱序/重连后可重新 GET 恢复。
- Contract Fixtures 通过。
- 无页面专用 DTO。

## 阶段报告

```text
reports/v1.4-node-control/phases/p12-completion.md
reports/v1.4-node-control/phases/p12-completion.json
reports/v1.4-node-control/phases/p12-handoff.json
```

报告必须记录 Base SHA、Implementation SHA、Evidence SHA、Remote SHA、实际测试、失败尝试、已知限制和下一阶段前置条件。
