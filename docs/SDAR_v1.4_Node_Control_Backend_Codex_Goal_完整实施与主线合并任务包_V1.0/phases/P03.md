# P03 — LLM Provider 与 Model Route 治理

## 建议实施提交

```text
feat(v1.4-p03): add llm provider and model route governance
```

证据提交：

```text
docs(v1.4-p03): record completion evidence
```

## 工作
1. LLM Provider Definition、Model Catalog、SecretRef。
2. Stage/Task/Case Model Route 与 Fallback Chain。
3. Structured Output/Tool Calling/Embedding 能力描述。
4. Health、Budget、Rate Limit、Timeout、Fallback Policy。
5. Apply/Ack，新配置默认只影响新 Task。
6. 禁止 Secret 进入 API、日志、审计和 Error。

## 验收
- Secret Scan 通过。
- Route Conflict/Unavailable/Fallback 有测试。
- 旧 Task 版本稳定。
- Runtime Ack 与 Observed 正确。

## 阶段报告

```text
reports/v1.4-node-control/phases/p03-completion.md
reports/v1.4-node-control/phases/p03-completion.json
reports/v1.4-node-control/phases/p03-handoff.json
```

报告必须记录 Base SHA、Implementation SHA、Evidence SHA、Remote SHA、实际测试、失败尝试、已知限制和下一阶段前置条件。
