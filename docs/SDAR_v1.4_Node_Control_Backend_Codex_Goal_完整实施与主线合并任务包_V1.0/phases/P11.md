# P11 — Telemetry Export

## 建议实施提交

```text
feat(v1.4-p11): add telemetry export control
```

证据提交：

```text
docs(v1.4-p11): record completion evidence
```

## 工作
1. TelemetryExportConfiguration/Status。
2. Endpoint、Source、CredentialRef、TLS、Record Families、Batch/Retry/Buffer。
3. Outbox Delivery Status、Last ACK、Pending、Oldest、Last Error。
4. Apply/Ack/LKG 和 Endpoint outage。
5. 平台不可用不影响 Task，达到高水位后保守 Retry/Retention。
6. 明确无 Query、Timeline、Evaluation、Reconciliation、ClickHouse。

## 验收
- Node API 无遥测查询路径。
- Telemetry outage 不停止 Runtime。
- Secret 不泄露。
- 本地发送状态可审计。

## 阶段报告

```text
reports/v1.4-node-control/phases/p11-completion.md
reports/v1.4-node-control/phases/p11-completion.json
reports/v1.4-node-control/phases/p11-handoff.json
```

报告必须记录 Base SHA、Implementation SHA、Evidence SHA、Remote SHA、实际测试、失败尝试、已知限制和下一阶段前置条件。
