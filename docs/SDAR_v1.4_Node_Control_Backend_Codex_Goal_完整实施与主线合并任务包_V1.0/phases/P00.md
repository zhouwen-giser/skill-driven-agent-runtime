# P00 — 最新 main 基线与 Goal 建立

## 建议实施提交

```text
docs(v1.4): establish latest-main implementation baseline
```

证据提交：

```text
docs(v1.4-p00): record completion evidence
```

## 工作
1. 获取并 Fast-forward 到最新 origin/main。
2. 验证 gh/auth/repository/default branch/clean worktree。
3. 运行未修改 main 的 pnpm install --frozen-lockfile 与 pnpm verify。
4. 确认 v1.3 P00-P13 基础已经进入 main。
5. 提取并验证两个冻结包，记录 SHA 与 Manifest。
6. 建立 ExecPlan、Source Lock、Object/Symbol/Authority/Migration/API Map。
7. 创建或恢复 feature/v1.4-node-control-backend 并推送。
8. 创建 goal-state.json、baseline reports、failed-attempts 目录。

## 验收
- 最新 main 精确 SHA 已记录。
- 分支基于该 SHA。
- 分支远程存在且 SHA 一致。
- 没有生产功能改动。
- 红色 main 按 Blocker 流程停止。

## 阶段报告

```text
reports/v1.4-node-control/phases/p00-completion.md
reports/v1.4-node-control/phases/p00-completion.json
reports/v1.4-node-control/phases/p00-handoff.json
```

报告必须记录 Base SHA、Implementation SHA、Evidence SHA、Remote SHA、实际测试、失败尝试、已知限制和下一阶段前置条件。
