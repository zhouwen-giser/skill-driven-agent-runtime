# P02 — Configuration Revision、Apply/Ack 与 LKG

## 建议实施提交

```text
feat(v1.4-p02): add revision apply ack and lkg control
```

证据提交：

```text
docs(v1.4-p02): record completion evidence
```

## 工作
1. 实现 ConfigurationRevision/Application、Desired/Observed。
2. 实现 Runtime Bootstrap/Latest/Watch/Apply/Ack。
3. 支持 hot_reload/new_task_only/reconnect_required/restart_required/immutable。
4. 实现 LKG 持久化、启动恢复、坏 Revision 拒绝、部分应用。
5. 使用 ETag/If-Match、Idempotency-Key、CAS。
6. 运行中 Task 不自动切换新版本。

## 验收
- Publish 不等于 Applied。
- Control Plane outage 时 Runtime 使用 LKG。
- 失败 Revision 不覆盖 Active/LKG。
- 并发发布只有一个成功。

## 阶段报告

```text
reports/v1.4-node-control/phases/p02-completion.md
reports/v1.4-node-control/phases/p02-completion.json
reports/v1.4-node-control/phases/p02-handoff.json
```

报告必须记录 Base SHA、Implementation SHA、Evidence SHA、Remote SHA、实际测试、失败尝试、已知限制和下一阶段前置条件。
