# G06：Planning Correction Facts 与 Interaction Episode

## 依赖

```text
G04、G05
```

## 阶段与估算

```text
Phase: P2
Estimated engineering effort: 6～8 person-days
```

## 目标

完成本 Goal 对应的产品实现、测试、文档、证据和提交，并保持 v1.2.2 权威不变。

## 开源复用

Gemini Auto Memory 的 User Correction 高权重；Claude Auto Memory 的作用域思想

必须遵守 `SOURCE-REUSE-POLICY.md`。任何实际复制或翻译代码都需要 Source Intake Report。

## 代码与数据范围

**Preferred paths**（若当前仓库已有等价结构，保留当前约定并在 ExecPlan 记录映射）：

- `packages/domain/src/cognitive/planning-correction.ts`
- `packages/application/src/cognitive/planning-correction-service.ts`
- `packages/application/src/cognitive/planning-interaction-episode-builder.ts`
- `packages/persistence-postgres/src/cognitive/planning-correction-repository.ts`
- `infra/postgres/migrations/*_v123_planning_corrections.sql`

**Required interfaces / services：**

- `PlanningCorrectionService.record`
- `PlanningInteractionEpisodeBuilder.build`
- `PlanningPreferenceProjector.projectLowRisk`

**Tables / persistence：**

`planning_correction_fact`、`planning_interaction_episode`

**Domain / Outbox events：**

- `planning.correction_recorded`

**API / A2A：**

- `GET /api/v1/tasks/{taskId}/planning-interactions`

**Model stages：**

- none

## 必须完成的实施任务

1. 记录 Understanding、Contract、Plan 的 Before/User Instruction/Patch/After/Validation。
2. 实现 correctionType 首批分类。
3. 关联 accepted/rejected、最终 Outcome 和后续反例。
4. 区分 task_scoped、user_scoped、tenant_scoped、global_candidate。
5. 低风险 User Preference 可进入现有 Memory Projection；安全/授权类禁止。
6. 生成 Interaction Episode Hash 和完整度。
7. 为 Task Type/Heuristic Induction 提供确定性 Fingerprint 输入。

## 实施细则

- 先审计当前仓库符号和测试，禁止创建重复服务或第二权威；
- 所有新 Domain 对象必须有 factory/validator、immutable snapshot 和明确 error code；
- 所有模型输出必须使用 Zod/JSON Schema，并由确定性 Application 代码校验；
- PostgreSQL 写入必须定义事务、CAS/幂等和重启语义；
- Redis/BullMQ 数据必须可由 PostgreSQL 重建；
- 适合时先加入失败测试，再完成实现；
- 更新 OpenAPI、Acceptance Map、Architecture、Migration 和 Console（如本 Goal 涉及）；
- 不允许 TODO、空实现、仅接口或只在测试环境生效的产品路径。

## 禁止事项

- 不改写 v1.2.2 Goal/Skill/Outcome/Recovery Authority；
- 不让 LLM 直接写权威状态；
- 不保存私有思维链；
- 不引入 Python Sidecar 或文件型知识权威；
- 不让 Candidate 绕过 Promotion；
- 不自动发布 Skill；
- 不删除失败测试或降低冻结验收标准；
- 不修改与本 Goal 无关的功能，除非为修复真实架构阻断并记录 ADR。

## 验证

- 一次用户修订不会自动全局化。
- 同一修订重复提交幂等。
- 最终 Outcome 补齐后生成新 Revision。
- 用户删除请求可传播到 scoped projection。

最低还必须运行受影响范围的 format、lint、typecheck、unit、contract、integration 和 build；最终完整 `pnpm verify` 在 G17 执行。

## Acceptance IDs

- `AC-G06-01`
- `AC-G06-02`
- `AC-G06-03`
- `AC-G06-04`
- `AC-G06-05`
- `AC-G06-06`

## 证据交付

- `reports/goal/g06-completion.md`；
- Acceptance ID → Test/Report 映射；
- 精确命令、结果、持续时间和日志路径；
- Migration/Schema/Fixture（如适用）；
- Source Intake 和第三方许可证证据（如适用）；
- 失败尝试、根因和修复；
- Commit SHA、Push 状态、Draft PR 更新；
- 当前工作树状态和下一 Goal Handoff。

## 完成合同

- 所有用户修订可追踪到前后版本和最终结果。
- 学习流程不依赖私有思维链。

并且：

```text
Implementation = complete
Tests = green or honestly blocked with evidence
Documentation = updated
Evidence = reproducible
Commit = meaningful and pushed
ExecPlan/sync-state = updated
```

## 下一 Goal 交接

将新增 Domain、Port、Schema、Migration、事件、API、测试 Fixture、已知限制和阻断写入 Master ExecPlan。不要仅在聊天或 PR 评论中保存关键交接信息。
