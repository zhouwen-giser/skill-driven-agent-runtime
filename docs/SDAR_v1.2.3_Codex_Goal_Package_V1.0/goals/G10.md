# G10：Task Type Induction

## 依赖

```text
G06、G09
```

## 阶段与估算

```text
Phase: P4
Estimated engineering effort: 8～11 person-days
```

## 目标

完成本 Goal 对应的产品实现、测试、文档、证据和提交，并保持 v1.2.2 权威不变。

## 开源复用

AWM Workflow Induction；AutoSkill Job-to-be-done Identity 和 Recent Intent Boundary

必须遵守 `SOURCE-REUSE-POLICY.md`。任何实际复制或翻译代码都需要 Source Intake Report。

## 代码与数据范围

**Preferred paths**（若当前仓库已有等价结构，保留当前约定并在 ExecPlan 记录映射）：

- `packages/domain/src/cognitive/task-type.ts`
- `packages/application/src/cognitive/task-type-induction-service.ts`
- `packages/application/src/cognitive/task-type-fingerprint.ts`
- `packages/persistence-postgres/src/cognitive/task-type-repository.ts`
- `infra/postgres/migrations/*_v123_task_types.sql`
- `tests/replay/task-type/**`

**Required interfaces / services：**

- `TaskTypeFingerprintBuilder.build`
- `TaskTypeClusterer.cluster`
- `TaskTypeInductionService.induce`
- `TaskTypeApplicabilityGuard.evaluate`

**Tables / persistence：**

`task_type_definition`、`task_type_evidence`

**Domain / Outbox events：**

- `knowledge.candidate_created`

**API / A2A：**

- `GET /api/v1/task-types`

**Model stages：**

- `task_type_induction`

## 必须完成的实施任务

1. 构建多维 Fingerprint：语义目标、Criteria、Artifact、Capability、DAG Shape、Correction、Outcome。
2. 先确定性聚类，再由 Model 命名、归纳和生成 Negative Examples。
3. 输出 Recognition、Required/Optional Dimensions、Criteria Template、Capability Requirement、Goal Pattern、Dependency Pattern。
4. 支持 Offline Batch 和 Online Candidate 两种模式。
5. 保存抽象 Task Type + 1～3 个具体 Episode Exemplars。
6. 一次 Episode 不创建 Active Task Type。
7. Task Type 与当前用户约束冲突时由 Applicability Guard 剔除。

## 实施细则

- 先审计当前仓库符号和测试，禁止创建重复服务或第二权威；
- 所有新 Domain 对象必须有 factory/validator、immutable snapshot 和明确 error code；
- 所有模型输出必须使用 Zod/JSON Schema，并由确定性 Application 代码校验；
- PostgreSQL 写入必须定义事务、CAS/幂等和重启语义；
- Redis/BullMQ 数据必须可由 PostgreSQL 重建；
- 适合时先加入失败测试，再完成实现；
- 更新 OpenAPI、Acceptance Map、Architecture、Migration 和 Console（如本 Goal 涉及）；
- 不允许 TODO、空实现、仅接口或只在测试环境生效的产品路径。

## 禁止事项

- 不改写 v1.2.2 Goal/Skill/Outcome/Recovery Authority；
- 不让 LLM 直接写权威状态；
- 不保存私有思维链；
- 不引入 Python Sidecar 或文件型知识权威；
- 不让 Candidate 绕过 Promotion；
- 不自动发布 Skill；
- 不删除失败测试或降低冻结验收标准；
- 不修改与本 Goal 无关的功能，除非为修复真实架构阻断并记录 ADR。

## 验证

- 相似目标但不同完成标准不会错误合并。
- 不同表述可识别同一 Task Type Candidate。
- Negative Example 能阻止误匹配。
- 人工 Fixture 与归纳 Candidate 可并存。

最低还必须运行受影响范围的 format、lint、typecheck、unit、contract、integration 和 build；最终完整 `pnpm verify` 在 G17 执行。

## Acceptance IDs

- `AC-G10-01`
- `AC-G10-02`
- `AC-G10-03`
- `AC-G10-04`
- `AC-G10-05`
- `AC-G10-06`

## 证据交付

- `reports/goal/g10-completion.md`；
- Acceptance ID → Test/Report 映射；
- 精确命令、结果、持续时间和日志路径；
- Migration/Schema/Fixture（如适用）；
- Source Intake 和第三方许可证证据（如适用）；
- 失败尝试、根因和修复；
- Commit SHA、Push 状态、Draft PR 更新；
- 当前工作树状态和下一 Goal Handoff。

## 完成合同

- 生成版本化 Candidate Task Type。
- 未晋升 Candidate 不影响正式 Understanding。

并且：

```text
Implementation = complete
Tests = green or honestly blocked with evidence
Documentation = updated
Evidence = reproducible
Commit = meaningful and pushed
ExecPlan/sync-state = updated
```

## 下一 Goal 交接

将新增 Domain、Port、Schema、Migration、事件、API、测试 Fixture、已知限制和阻断写入 Master ExecPlan。不要仅在聊天或 PR 评论中保存关键交接信息。
