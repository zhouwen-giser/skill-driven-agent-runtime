# G17：Hardening、灰度与 Release

## 依赖

```text
G00～G16
```

## 阶段与估算

```text
Phase: P7
Estimated engineering effort: 15～20 person-days
```

## 目标

完成本 Goal 对应的产品实现、测试、文档、证据和提交，并保持 v1.2.2 权威不变。

## 开源复用

现有 `verify`、A2A TCK、SBOM、sources lock 和架构检查流水线

必须遵守 `SOURCE-REUSE-POLICY.md`。任何实际复制或翻译代码都需要 Source Intake Report。

## 代码与数据范围

**Preferred paths**（若当前仓库已有等价结构，保留当前约定并在 ExecPlan 记录映射）：

- `scripts/verify-v123.mjs`
- `scripts/reset-v123-database.mjs`
- `scripts/check-v123-acceptance.mjs`
- `scripts/generate-sbom.mjs`
- `infra/**`
- `reports/v1.2.3-release/**`
- `docs/releases/v1.2.3.md`

**Required interfaces / services：**

- `CognitiveRuntimeReconciler.rebuild`
- `RetentionService.apply`
- `DeletionPropagationService.propagate`
- `FeatureRolloutPolicy.evaluate`

**Tables / persistence：**

全量索引、Retention、清理、重建和容量验证

**Domain / Outbox events：**

- none

**API / A2A：**

- none

**Model stages：**

- none

## 必须完成的实施任务

1. 完成并发、CAS、幂等、Lease、Outbox、Redis 清空和重启恢复。
2. 完成租户/用户作用域、PII 删除传播、Prompt Injection 和 Public Card 隐私审计。
3. 完成容量、Backpressure、Queue Lag、Model Budget 和 Retention 测试。
4. 实现 Capture→Observe→Candidate→Shadow→Advisory→Active Low-risk 灰度。
5. 默认：Summary/Card on、Understanding ambiguous、Interactive manual、Capture/Observer on、Induction shadow、Promotion manual、Injection shadow。
6. 更新 Architecture、OpenAPI、Acceptance Map、Migration、Source Lock、SBOM 和 THIRD_PARTY_NOTICES。
7. 运行 unit/contract/integration/e2e/A2A TCK/chaos/replay/security。
8. 生成 Release Report，明确 Experience=Advisory、无 Python Sidecar、无自动 Skill 发布。

## 实施细则

- 先审计当前仓库符号和测试，禁止创建重复服务或第二权威；
- 所有新 Domain 对象必须有 factory/validator、immutable snapshot 和明确 error code；
- 所有模型输出必须使用 Zod/JSON Schema，并由确定性 Application 代码校验；
- PostgreSQL 写入必须定义事务、CAS/幂等和重启语义；
- Redis/BullMQ 数据必须可由 PostgreSQL 重建；
- 适合时先加入失败测试，再完成实现；
- 更新 OpenAPI、Acceptance Map、Architecture、Migration 和 Console（如本 Goal 涉及）；
- 不允许 TODO、空实现、仅接口或只在测试环境生效的产品路径。

## 禁止事项

- 不改写 v1.2.2 Goal/Skill/Outcome/Recovery Authority；
- 不让 LLM 直接写权威状态；
- 不保存私有思维链；
- 不引入 Python Sidecar 或文件型知识权威；
- 不让 Candidate 绕过 Promotion；
- 不自动发布 Skill；
- 不删除失败测试或降低冻结验收标准；
- 不修改与本 Goal 无关的功能，除非为修复真实架构阻断并记录 ADR。

## 验证

- 40 项总体架构场景和上位需求 50 项核心场景。
- P95 性能目标。
- 跨租户和删除传播。
- Worker/DB/Redis/Model 故障降级。
- 全量 `pnpm verify`。

最低还必须运行受影响范围的 format、lint、typecheck、unit、contract、integration 和 build；最终完整 `pnpm verify` 在 G17 执行。

## Acceptance IDs

- `AC-G17-01`
- `AC-G17-02`
- `AC-G17-03`
- `AC-G17-04`
- `AC-G17-05`
- `AC-G17-06`
- `AC-G17-07`
- `AC-G17-08`
- `AC-G17-09`

## 证据交付

- `reports/goal/g17-completion.md`；
- Acceptance ID → Test/Report 映射；
- 精确命令、结果、持续时间和日志路径；
- Migration/Schema/Fixture（如适用）；
- Source Intake 和第三方许可证证据（如适用）；
- 失败尝试、根因和修复；
- Commit SHA、Push 状态、Draft PR 更新；
- 当前工作树状态和下一 Goal Handoff。

## 完成合同

- 全部发布门禁通过。
- v1.2.2 原执行链无回归。
- 默认配置不自动启用高风险知识。

并且：

```text
Implementation = complete
Tests = green or honestly blocked with evidence
Documentation = updated
Evidence = reproducible
Commit = meaningful and pushed
ExecPlan/sync-state = updated
```

## 下一 Goal 交接

将新增 Domain、Port、Schema、Migration、事件、API、测试 Fixture、已知限制和阻断写入 Master ExecPlan。不要仅在聊天或 PR 评论中保存关键交接信息。
