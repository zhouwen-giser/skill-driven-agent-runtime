# G16：Evaluation、Replay 与 Shadow Harness

## 依赖

```text
G03、G05、G08、G12、G14
```

## 阶段与估算

```text
Phase: P6
Estimated engineering effort: 12～16 person-days
```

## 目标

完成本 Goal 对应的产品实现、测试、文档、证据和提交，并保持 v1.2.2 权威不变。

## 开源复用

AutoSkill SkillEvo mutate_dev/promotion_test；ACE pre/post；AWM 轨迹数据结构

必须遵守 `SOURCE-REUSE-POLICY.md`。任何实际复制或翻译代码都需要 Source Intake Report。

## 代码与数据范围

**Preferred paths**（若当前仓库已有等价结构，保留当前约定并在 ExecPlan 记录映射）：

- `tests/replay/cognitive/**`
- `packages/application/src/cognitive/shadow-planning-service.ts`
- `scripts/run-cognitive-replay.mjs`
- `scripts/generate-promotion-report.mjs`
- `reports/v1.2.3-replay/**`

**Required interfaces / services：**

- `PlanningReplayDatasetBuilder.build`
- `ShadowPlanningService.compare`
- `PromotionReportGenerator.generate`
- `NoPhysicalProvider.assertNoSideEffects`

**Tables / persistence：**

Replay Dataset/Run/Result 可使用专用测试表或版本化 Artifact；不得写生产 Active Knowledge

**Domain / Outbox events：**

- none

**API / A2A：**

- none

**Model stages：**

- none

## 必须完成的实施任务

1. 建立 Planning Replay Dataset：request、world summary、accepted contract/plan、patch、outcome、catalogHash、knowledge。
2. 实现 Understanding、Contract、Plan、Injection、Task Type Recognition 和 Gap Replay。
3. 数据分为 mutate_dev 和 promotion_test，防止同数据优化和晋升。
4. Baseline/Champion/Candidate 比较，Hard Failure 不允许退化。
5. 实现 Shadow Planning，输出 improved/neutral/regressed/invalid/unsafe。
6. 构建 Evaluation Metrics：Missing Dimension、Coverage、Patch Count、Attempt、Recovery、Risk、Token/Latency。
7. Replay 使用 No Physical Provider；禁止真实 MCP 副作用。
8. 生成 Promotion Provenance Report。

## 实施细则

- 先审计当前仓库符号和测试，禁止创建重复服务或第二权威；
- 所有新 Domain 对象必须有 factory/validator、immutable snapshot 和明确 error code；
- 所有模型输出必须使用 Zod/JSON Schema，并由确定性 Application 代码校验；
- PostgreSQL 写入必须定义事务、CAS/幂等和重启语义；
- Redis/BullMQ 数据必须可由 PostgreSQL 重建；
- 适合时先加入失败测试，再完成实现；
- 更新 OpenAPI、Acceptance Map、Architecture、Migration 和 Console（如本 Goal 涉及）；
- 不允许 TODO、空实现、仅接口或只在测试环境生效的产品路径。

## 禁止事项

- 不改写 v1.2.2 Goal/Skill/Outcome/Recovery Authority；
- 不让 LLM 直接写权威状态；
- 不保存私有思维链；
- 不引入 Python Sidecar 或文件型知识权威；
- 不让 Candidate 绕过 Promotion；
- 不自动发布 Skill；
- 不删除失败测试或降低冻结验收标准；
- 不修改与本 Goal 无关的功能，除非为修复真实架构阻断并记录 ADR。

## 验证

- Replay 确定性和数据隔离。
- Candidate 无样本进入 incubating。
- Hard Failure 退化时拒绝晋升。
- Replay 不修改生产 Skill/Knowledge。

最低还必须运行受影响范围的 format、lint、typecheck、unit、contract、integration 和 build；最终完整 `pnpm verify` 在 G17 执行。

## Acceptance IDs

- `AC-G16-01`
- `AC-G16-02`
- `AC-G16-03`
- `AC-G16-04`
- `AC-G16-05`
- `AC-G16-06`
- `AC-G16-07`

## 证据交付

- `reports/goal/g16-completion.md`；
- Acceptance ID → Test/Report 映射；
- 精确命令、结果、持续时间和日志路径；
- Migration/Schema/Fixture（如适用）；
- Source Intake 和第三方许可证证据（如适用）；
- 失败尝试、根因和修复；
- Commit SHA、Push 状态、Draft PR 更新；
- 当前工作树状态和下一 Goal Handoff。

## 完成合同

- 每次 Promotion 都有可复验报告。
- Shadow 结果不影响正式任务。
- 可量化开源算法复用收益。

并且：

```text
Implementation = complete
Tests = green or honestly blocked with evidence
Documentation = updated
Evidence = reproducible
Commit = meaningful and pushed
ExecPlan/sync-state = updated
```

## 下一 Goal 交接

将新增 Domain、Port、Schema、Migration、事件、API、测试 Fixture、已知限制和阻断写入 Master ExecPlan。不要仅在聊天或 PR 评论中保存关键交接信息。
