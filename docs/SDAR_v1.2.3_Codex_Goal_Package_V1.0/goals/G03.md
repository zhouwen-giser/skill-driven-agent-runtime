# G03：Generic Task Understanding

## 依赖

```text
G00、G01；初始 Task Type 使用人工 Fixture
```

## 阶段与估算

```text
Phase: P2
Estimated engineering effort: 9～12 person-days
```

## 目标

完成本 Goal 对应的产品实现、测试、文档、证据和提交，并保持 v1.2.2 权威不变。

## 开源复用

Claude Code Plan Mode 的只读理解；LangMem Typed Extraction

必须遵守 `SOURCE-REUSE-POLICY.md`。任何实际复制或翻译代码都需要 Source Intake Report。

## 代码与数据范围

**Preferred paths**（若当前仓库已有等价结构，保留当前约定并在 ExecPlan 记录映射）：

- `packages/domain/src/cognitive/task-understanding.ts`
- `packages/application/src/cognitive/cognitive-entry-router.ts`
- `packages/application/src/cognitive/generic-task-understanding-service.ts`
- `packages/persistence-postgres/src/cognitive/task-understanding-repository.ts`
- `infra/postgres/migrations/*_v123_task_understanding.sql`
- `tests/contract/model/task-understanding*.test.ts`

**Required interfaces / services：**

- `CognitiveEntryRouter.route`
- `GenericTaskUnderstandingService.understand`
- `TaskTypeIndexSource.search`
- `TaskUnderstandingRepository.saveRevision`

**Tables / persistence：**

`generic_task_understanding`、`generic_task_understanding_dimension`

**Domain / Outbox events：**

- `task.understanding_created`

**API / A2A：**

- `GET /api/v1/tasks/{taskId}/understanding`
- `GET /api/v1/tasks/{taskId}/understanding/revisions`

**Model stages：**

- `task_understanding`

## 必须完成的实施任务

1. 实现 `CognitiveEntryRouter`，区分明确任务和泛型任务。
2. 实现 `GenericTaskUnderstandingService` 和 `task_understanding` Model Stage。
3. 定义 Task Type Candidate、Capability Requirement、Known Constraint、Missing Dimension、Assumption。
4. 实现 Blocking/Conditional/Non-blocking Dimension 分类。
5. 实现 target、scope、time_range、criteria、artifact、evidence、side_effect_authorization 等首批维度。
6. 加载 Capability Index、人工 Task Type Index 和低风险 User Preference。
7. 实现高风险授权不明时的 `confirmation_required`。
8. 将外部文本包裹为数据，防止 Prompt Injection。
9. 保存 Understanding Revision、Source Refs、Policy Version、Model Invocation 和 State Hash。

## 实施细则

- 先审计当前仓库符号和测试，禁止创建重复服务或第二权威；
- 所有新 Domain 对象必须有 factory/validator、immutable snapshot 和明确 error code；
- 所有模型输出必须使用 Zod/JSON Schema，并由确定性 Application 代码校验；
- PostgreSQL 写入必须定义事务、CAS/幂等和重启语义；
- Redis/BullMQ 数据必须可由 PostgreSQL 重建；
- 适合时先加入失败测试，再完成实现；
- 更新 OpenAPI、Acceptance Map、Architecture、Migration 和 Console（如本 Goal 涉及）；
- 不允许 TODO、空实现、仅接口或只在测试环境生效的产品路径。

## 禁止事项

- 不改写 v1.2.2 Goal/Skill/Outcome/Recovery Authority；
- 不让 LLM 直接写权威状态；
- 不保存私有思维链；
- 不引入 Python Sidecar 或文件型知识权威；
- 不让 Candidate 绕过 Promotion；
- 不自动发布 Skill；
- 不删除失败测试或降低冻结验收标准；
- 不修改与本 Goal 无关的功能，除非为修复真实架构阻断并记录 ADR。

## 验证

- 模糊任务进入 Understanding。
- 明确任务可直接形成 Contract Candidate。
- 缺目标、范围和标准时召回正确维度。
- 安全授权缺失不会被经验静默填充。
- 结构化输出非法时有界重试。

最低还必须运行受影响范围的 format、lint、typecheck、unit、contract、integration 和 build；最终完整 `pnpm verify` 在 G17 执行。

## Acceptance IDs

- `AC-G03-01`
- `AC-G03-02`
- `AC-G03-03`
- `AC-G03-04`
- `AC-G03-05`
- `AC-G03-06`

## 证据交付

- `reports/goal/g03-completion.md`；
- Acceptance ID → Test/Report 映射；
- 精确命令、结果、持续时间和日志路径；
- Migration/Schema/Fixture（如适用）；
- Source Intake 和第三方许可证证据（如适用）；
- 失败尝试、根因和修复；
- Commit SHA、Push 状态、Draft PR 更新；
- 当前工作树状态和下一 Goal Handoff。

## 完成合同

- 能够输出四种 disposition。
- Task Type 只辅助理解，不覆盖用户表达。
- P95 目标≤3s（不含用户等待）。

并且：

```text
Implementation = complete
Tests = green or honestly blocked with evidence
Documentation = updated
Evidence = reproducible
Commit = meaningful and pushed
ExecPlan/sync-state = updated
```

## 下一 Goal 交接

将新增 Domain、Port、Schema、Migration、事件、API、测试 Fixture、已知限制和阻断写入 Master ExecPlan。不要仅在聊天或 PR 评论中保存关键交接信息。
