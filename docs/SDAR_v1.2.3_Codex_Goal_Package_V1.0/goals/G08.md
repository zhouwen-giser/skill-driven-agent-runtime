# G08：Experience Observer 与 Typed Extractors

## 依赖

```text
G07
```

## 阶段与估算

```text
Phase: P3
Estimated engineering effort: 9～12 person-days
```

## 目标

完成本 Goal 对应的产品实现、测试、文档、证据和提交，并保持 v1.2.2 权威不变。

## 开源复用

LangMem Typed Extractor/Consolidation；Gemini Evidence-only/No-op/Redaction

必须遵守 `SOURCE-REUSE-POLICY.md`。任何实际复制或翻译代码都需要 Source Intake Report。

## 代码与数据范围

**Preferred paths**（若当前仓库已有等价结构，保留当前约定并在 ExecPlan 记录映射）：

- `packages/domain/src/cognitive/observation.ts`
- `packages/application/src/cognitive/experience-observer-service.ts`
- `packages/application/src/cognitive/experience-extractor-pipeline.ts`
- `packages/application/src/cognitive/extractors/*.ts`
- `packages/persistence-postgres/src/cognitive/observation-repository.ts`
- `infra/postgres/migrations/*_v123_observation.sql`

**Required interfaces / services：**

- `ExperienceExtractor<T>.extract`
- `ExperienceExtractorPipeline.run`
- `ExperienceObserverService.observe`
- `ObservationRepository.save`

**Tables / persistence：**

`experience_observation`、`experience_observation_fact`、`experience_extraction`

**Domain / Outbox events：**

- `experience.observation_completed`

**API / A2A：**

- `GET /api/v1/experience/observations`

**Model stages：**

- `experience_observation`

## 必须完成的实施任务

1. 定义通用 `ExperienceExtractor<T>` Port 和独立失败语义。
2. 实现 GoalPattern、TaskTypeSignal、Decomposition、Dependency、Criterion、Evidence、Artifact、Capability、Failure、Recovery、NoProgress、HumanCorrection Extractor。
3. Observer 输入按 Contract/Plan/Attempt/Outcome/Recovery/Correction 分区。
4. 输出区分 fact、inference、candidate_lesson、uncertainty、contradiction。
5. 实现 bounded multi-step Consolidation，已有 Observation 只能生成变更建议。
6. 实现 Batch、Token/Byte Budget、Fast/Reasoning Model 分级。
7. 模型输出经 Zod/AJV、引用存在性、长度、枚举和注入清洗。
8. Extractor 单项失败不使整个 Observation 失败。

## 实施细则

- 先审计当前仓库符号和测试，禁止创建重复服务或第二权威；
- 所有新 Domain 对象必须有 factory/validator、immutable snapshot 和明确 error code；
- 所有模型输出必须使用 Zod/JSON Schema，并由确定性 Application 代码校验；
- PostgreSQL 写入必须定义事务、CAS/幂等和重启语义；
- Redis/BullMQ 数据必须可由 PostgreSQL 重建；
- 适合时先加入失败测试，再完成实现；
- 更新 OpenAPI、Acceptance Map、Architecture、Migration 和 Console（如本 Goal 涉及）；
- 不允许 TODO、空实现、仅接口或只在测试环境生效的产品路径。

## 禁止事项

- 不改写 v1.2.2 Goal/Skill/Outcome/Recovery Authority；
- 不让 LLM 直接写权威状态；
- 不保存私有思维链；
- 不引入 Python Sidecar 或文件型知识权威；
- 不让 Candidate 绕过 Promotion；
- 不自动发布 Skill；
- 不删除失败测试或降低冻结验收标准；
- 不修改与本 Goal 无关的功能，除非为修复真实架构阻断并记录 ADR。

## 验证

- 12 个 Extractor 的 Schema Golden Test。
- 单个 Extractor 失败其余仍保存。
- Transcript 指令不会被当作系统指令。
- 无足够证据时输出 no-op。
- Observer 重试和 Dead Letter。

最低还必须运行受影响范围的 format、lint、typecheck、unit、contract、integration 和 build；最终完整 `pnpm verify` 在 G17 执行。

## Acceptance IDs

- `AC-G08-01`
- `AC-G08-02`
- `AC-G08-03`
- `AC-G08-04`
- `AC-G08-05`
- `AC-G08-06`
- `AC-G08-07`

## 证据交付

- `reports/goal/g08-completion.md`；
- Acceptance ID → Test/Report 映射；
- 精确命令、结果、持续时间和日志路径；
- Migration/Schema/Fixture（如适用）；
- Source Intake 和第三方许可证证据（如适用）；
- 失败尝试、根因和修复；
- Commit SHA、Push 状态、Draft PR 更新；
- 当前工作树状态和下一 Goal Handoff。

## 完成合同

- Observation 可追踪到 Episode 和 Model Invocation。
- 不生成无来源 Candidate。
- 后台失败不影响在线任务。

并且：

```text
Implementation = complete
Tests = green or honestly blocked with evidence
Documentation = updated
Evidence = reproducible
Commit = meaningful and pushed
ExecPlan/sync-state = updated
```

## 下一 Goal 交接

将新增 Domain、Port、Schema、Migration、事件、API、测试 Fixture、已知限制和阻断写入 Master ExecPlan。不要仅在聊天或 PR 评论中保存关键交接信息。
