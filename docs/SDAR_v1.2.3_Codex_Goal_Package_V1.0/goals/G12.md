# G12：Knowledge Promotion Framework

## 依赖

```text
G09、G10、G11；复用现有 Skill Evolution 的通用验证组件
```

## 阶段与估算

```text
Phase: P5
Estimated engineering effort: 12～16 person-days
```

## 目标

完成本 Goal 对应的产品实现、测试、文档、证据和提交，并保持 v1.2.2 权威不变。

## 开源复用

AutoSkill SkillEvo Lineage/Champion/Replay；ACE 正反计数；Gemini Candidate Inbox

必须遵守 `SOURCE-REUSE-POLICY.md`。任何实际复制或翻译代码都需要 Source Intake Report。

## 代码与数据范围

**Preferred paths**（若当前仓库已有等价结构，保留当前约定并在 ExecPlan 记录映射）：

- `packages/domain/src/cognitive/promotion.ts`
- `packages/application/src/cognitive/knowledge-promotion-service.ts`
- `packages/application/src/cognitive/promotion-targets/*.ts`
- `packages/application/src/cognitive/replay-evaluation-runner.ts`
- `packages/persistence-postgres/src/cognitive/promotion-repository.ts`
- `infra/postgres/migrations/*_v123_promotion.sql`

**Required interfaces / services：**

- `KnowledgePromotionService.evaluate`
- `EvidenceThresholdEvaluator.evaluate`
- `DuplicateCandidateDetector.find`
- `ReplayEvaluationRunner.run`
- `PromotionTarget.promote`
- `ActiveKnowledgeProjector.project`

**Tables / persistence：**

`planning_heuristic`、`planning_heuristic_evidence`、`knowledge_promotion_evaluation`、`knowledge_status_transition`

**Domain / Outbox events：**

- `knowledge.validating`
- `knowledge.promoted`
- `knowledge.rejected`

**API / A2A：**

- `POST /api/v1/knowledge/{kind}/{id}/promote`
- `POST /api/v1/knowledge/{kind}/{id}/reject`
- `POST /api/v1/knowledge/{kind}/{id}/revalidate`
- `POST /api/v1/knowledge/{kind}/{id}/deprecate`

**Model stages：**

- `knowledge_promotion_assessment`

## 必须完成的实施任务

1. 从现有 Skill Evolution 抽取 Threshold、Duplicate、Replay、Case Runner、Correction Diff 通用组件。
2. 为 Heuristic、Task Type、Capability Pattern 实现独立 Promotion Target Adapter。
3. 实现 candidate→validating→active→deprecated/rejected 状态机和 CAS。
4. 聚合 uniqueGoal、uniqueUser、success/failure、accepted/rejected、replay、shadow、support/contradiction。
5. 实现初始阈值策略和 policyVersion。
6. 高风险候选必须 Replay+Shadow+人工审核+Policy Allow。
7. 新反例、Catalog Hash、Policy、Skill Version 和拒绝率变化触发 active→validating。
8. Active Knowledge 投影到现有 MemoryService，只保存检索摘要与权威引用。
9. Promotion 与 Skill Publication 严格分离。

## 实施细则

- 先审计当前仓库符号和测试，禁止创建重复服务或第二权威；
- 所有新 Domain 对象必须有 factory/validator、immutable snapshot 和明确 error code；
- 所有模型输出必须使用 Zod/JSON Schema，并由确定性 Application 代码校验；
- PostgreSQL 写入必须定义事务、CAS/幂等和重启语义；
- Redis/BullMQ 数据必须可由 PostgreSQL 重建；
- 适合时先加入失败测试，再完成实现；
- 更新 OpenAPI、Acceptance Map、Architecture、Migration 和 Console（如本 Goal 涉及）；
- 不允许 TODO、空实现、仅接口或只在测试环境生效的产品路径。

## 禁止事项

- 不改写 v1.2.2 Goal/Skill/Outcome/Recovery Authority；
- 不让 LLM 直接写权威状态；
- 不保存私有思维链；
- 不引入 Python Sidecar 或文件型知识权威；
- 不让 Candidate 绕过 Promotion；
- 不自动发布 Skill；
- 不删除失败测试或降低冻结验收标准；
- 不修改与本 Goal 无关的功能，除非为修复真实架构阻断并记录 ADR。

## 验证

- 单 Episode 无法晋升。
- 阈值、反例比例和风险门禁。
- 一个 Candidate Version 只有一个终态 Evaluation。
- 人工拒绝/修订可追踪。
- Memory Projection 删除后可由权威表重建。

最低还必须运行受影响范围的 format、lint、typecheck、unit、contract、integration 和 build；最终完整 `pnpm verify` 在 G17 执行。

## Acceptance IDs

- `AC-G12-01`
- `AC-G12-02`
- `AC-G12-03`
- `AC-G12-04`
- `AC-G12-05`
- `AC-G12-06`
- `AC-G12-07`

## 证据交付

- `reports/goal/g12-completion.md`；
- Acceptance ID → Test/Report 映射；
- 精确命令、结果、持续时间和日志路径；
- Migration/Schema/Fixture（如适用）；
- Source Intake 和第三方许可证证据（如适用）；
- 失败尝试、根因和修复；
- Commit SHA、Push 状态、Draft PR 更新；
- 当前工作树状态和下一 Goal Handoff。

## 完成合同

- 只有 Active Knowledge 可被正式 Retriever 读取。
- 高风险不可自动晋升。
- 不存在 Memory/结构化知识双权威。

并且：

```text
Implementation = complete
Tests = green or honestly blocked with evidence
Documentation = updated
Evidence = reproducible
Commit = meaningful and pushed
ExecPlan/sync-state = updated
```

## 下一 Goal 交接

将新增 Domain、Port、Schema、Migration、事件、API、测试 Fixture、已知限制和阻断写入 Master ExecPlan。不要仅在聊天或 PR 评论中保存关键交接信息。
