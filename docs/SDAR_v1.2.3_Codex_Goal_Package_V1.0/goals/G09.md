# G09：Experience Reflector、Identity 与 Knowledge Curator

## 依赖

```text
G08
```

## 阶段与估算

```text
Phase: P4
Estimated engineering effort: 8～11 person-days
```

## 目标

完成本 Goal 对应的产品实现、测试、文档、证据和提交，并保持 v1.2.2 权威不变。

## 开源复用

ACE Reflector/Curator/Delta；AutoSkill Identity/Merge；LangMem Change Set

必须遵守 `SOURCE-REUSE-POLICY.md`。任何实际复制或翻译代码都需要 Source Intake Report。

## 代码与数据范围

**Preferred paths**（若当前仓库已有等价结构，保留当前约定并在 ExecPlan 记录映射）：

- `packages/domain/src/cognitive/knowledge-delta.ts`
- `packages/application/src/cognitive/experience-reflector-service.ts`
- `packages/application/src/cognitive/knowledge-identity-service.ts`
- `packages/application/src/cognitive/knowledge-curator-service.ts`
- `packages/persistence-postgres/src/cognitive/reflection-repository.ts`
- `infra/postgres/migrations/*_v123_reflection.sql`

**Required interfaces / services：**

- `ExperienceReflectorService.reflect`
- `KnowledgeIdentityService.compare`
- `KnowledgeCuratorService.proposeDelta`
- `KnowledgeDeltaValidator.validate`

**Tables / persistence：**

`experience_reflection`、通用 Candidate/Delta 记录（或各知识表的 candidate revision）

**Domain / Outbox events：**

- `experience.reflection_completed`
- `knowledge.candidate_created`
- `knowledge.contradiction_recorded`

**API / A2A：**

- `GET /api/v1/experience/reflections`

**Model stages：**

- `experience_reflection`

## 必须完成的实施任务

1. 实现 Generator/Reflector/Curator 分工，不保存原始 reasoning trace。
2. Reflector 评价知识对 Outcome、用户接受和 Validator 的 helpful/harmful/neutral 影响。
3. 实现语义相似度、名称/标签/目标重叠、Recent Intent Boundary 和去实例化 Identity。
4. Curator 只输出 CREATE_REVISION、SUGGEST_MERGE、SUGGEST_SUPERSEDE、ADD_EVIDENCE、ADD_CONTRADICTION、NO_CHANGE。
5. 所有 Delta 经确定性校验，模型不得直接应用 Active Knowledge。
6. 实现 Candidate Fingerprint、Duplicate Search、Merge/Supersede Lineage。
7. 无效 JSON、空结果和操作错误均 no-op 并继续。
8. Reflection 按 tenant、goalPattern、capabilityFingerprint、时间窗口分批。

## 实施细则

- 先审计当前仓库符号和测试，禁止创建重复服务或第二权威；
- 所有新 Domain 对象必须有 factory/validator、immutable snapshot 和明确 error code；
- 所有模型输出必须使用 Zod/JSON Schema，并由确定性 Application 代码校验；
- PostgreSQL 写入必须定义事务、CAS/幂等和重启语义；
- Redis/BullMQ 数据必须可由 PostgreSQL 重建；
- 适合时先加入失败测试，再完成实现；
- 更新 OpenAPI、Acceptance Map、Architecture、Migration 和 Console（如本 Goal 涉及）；
- 不允许 TODO、空实现、仅接口或只在测试环境生效的产品路径。

## 禁止事项

- 不改写 v1.2.2 Goal/Skill/Outcome/Recovery Authority；
- 不让 LLM 直接写权威状态；
- 不保存私有思维链；
- 不引入 Python Sidecar 或文件型知识权威；
- 不让 Candidate 绕过 Promotion；
- 不自动发布 Skill；
- 不删除失败测试或降低冻结验收标准；
- 不修改与本 Goal 无关的功能，除非为修复真实架构阻断并记录 ADR。

## 验证

- 同一 Job-to-be-done 合并，不同交付物/标准拆分。
- 具体设备、地点、日期去除后仍能判断复用边界。
- 低置信度 Identity 默认不合并。
- Curator 非法 Delta 不改变数据。
- 正反证据均保留。

最低还必须运行受影响范围的 format、lint、typecheck、unit、contract、integration 和 build；最终完整 `pnpm verify` 在 G17 执行。

## Acceptance IDs

- `AC-G09-01`
- `AC-G09-02`
- `AC-G09-03`
- `AC-G09-04`
- `AC-G09-05`
- `AC-G09-06`
- `AC-G09-07`

## 证据交付

- `reports/goal/g09-completion.md`；
- Acceptance ID → Test/Report 映射；
- 精确命令、结果、持续时间和日志路径；
- Migration/Schema/Fixture（如适用）；
- Source Intake 和第三方许可证证据（如适用）；
- 失败尝试、根因和修复；
- Commit SHA、Push 状态、Draft PR 更新；
- 当前工作树状态和下一 Goal Handoff。

## 完成合同

- Reflector 只能创建 Candidate/Revision。
- Candidate Lineage 可追踪。
- 反例不会被压缩丢失。

并且：

```text
Implementation = complete
Tests = green or honestly blocked with evidence
Documentation = updated
Evidence = reproducible
Commit = meaningful and pushed
ExecPlan/sync-state = updated
```

## 下一 Goal 交接

将新增 Domain、Port、Schema、Migration、事件、API、测试 Fixture、已知限制和阻断写入 Master ExecPlan。不要仅在聊天或 PR 评论中保存关键交接信息。
