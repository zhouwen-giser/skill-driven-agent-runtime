# G03：User Goal Contract 与 Skill Goal DAG Planning

## 依赖

```text
G02
```

## 目标

实现正式请求入口：User Goal Contract → Planning Node → Skill Goal DAG → Validator → Plan。

## 范围

- A2A/AgentTask入口；
- UserGoalContractService；
- `goal_planning` Stage；
- UserGoalPlanningService；
- SkillGoalPlanValidator；
- Goal Patch/Replan。

## 必须完成的修改

1. 实现 UserGoalCompletionContract。
2. 实现结构化 Planning Stage。
3. 输出 Goal、Dependency、Capability、Criterion、Effect、Evidence、Artifact、Assumption、Coverage。
4. 禁止 Skill/Tool/Provider/Workflow ID。
5. 实现 DAG/Coverage/Bounds/Version/Forbidden Replay Validator。
6. 两次非法后 fail closed/input/gap。
7. 实现 Plan Version、sourcePlanId、revisionKind、supersede。
8. 删除直接 Skill Selection 入口。

## 禁止事项

- 不改变冻结架构；
- 不用 TODO 或 Mock 代替产品实现；
- 不修改 Provider 项目；
- 不修改无关功能。

## 验证

- 单Goal/串行/并行 DAG。
- Cycle/Missing Dependency/Uncovered Criterion。
- Prompt注入ID拒绝。
- Goal Patch/Replan。
- Model invalid/timeout。

## 证据交付

- Planning fixtures
- Validator matrix
- Coverage report
- Model evidence

## 完成合同

- Required Coverage=100%；
- Plan 不绑定执行能力；
- Plan 版本化持久化；
- A2A 不直接进入 Skill Selection。

## 下一 Goal 交接

更新 Master ExecPlan，并将提交、测试、证据、阻断和权威输出交给下一 Goal。
