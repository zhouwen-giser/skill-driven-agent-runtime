# G02：v1.2.2 Baseline Schema 与 Domain

## 依赖

```text
G01
```

## 目标

建立可清库重建的 v1.2.2 数据库 Baseline、Domain、Repository、reset/seed，不支持 v1.2.1 数据库升级。

## 范围

- User Goal Contract/Plan；
- Skill Goal/DAG/Coverage；
- SkillAttempt/Execution Contract；
- Task/Skill/User Outcome；
- Progress/Completed Effect；
- Recovery；
- Business Event Client表；
- Workflow/RemoteTask新关联。

## 必须完成的修改

1. 创建 v1.2.2 Baseline Schema。
2. 实现所有 Goal Planning/Execution/Outcome/Recovery/Event Client 表。
3. WorkflowPlan/RemoteTaskBinding 绑定 skillGoal/attempt。
4. 建立唯一约束、CAS、幂等键。
5. 实现有界 JSONB。
6. 实现 `db:reset:v1.2.2`、development/test seed。
7. 更新 Domain 和 Repository。
8. 停止旧 Migration Runner 兼容链。

## 禁止事项

- 不改变冻结架构；
- 不用 TODO 或 Mock 代替产品实现；
- 不修改 Provider 项目；
- 不修改无关功能。

## 验证

- 空库 Baseline。
- Schema Hash。
- Repository round-trip。
- CAS/unique race。
- reset 环境保护。
- Seed 最小启动。

## 证据交付

- Baseline SQL/hash
- Domain schema report
- Repository report
- Reset/seed safety report

## 完成合同

- 空库可创建完整 v1.2.2；
- 无历史升级/Down Migration；
- Domain 状态机可测试；
- G03/G07 可使用正式 Repository。

## 下一 Goal 交接

更新 Master ExecPlan，并将提交、测试、证据、阻断和权威输出交给下一 Goal。
