# G00：基线、ExecPlan 与合同资产

## 依赖

```text
无
```

## 目标

确认 SDAR 当前真实基线和模块边界，创建完整 v1.2.2 ExecPlan、追踪矩阵和架构门禁。

## 范围

- `skill-driven-agent-runtime` 当前 `origin/main`；
- AGENTS/README/package scripts；
- 现有架构、Migration、OpenAPI、Console、报告；
- v1.2.2 Requirements/Design/Clean-slate Plan；
- Provider V0.5.2 冻结合同，只读。

## 必须完成的修改

1. 校验最低祖先和工作树。
2. 运行当前主干完整验证并记录基线。
3. 创建 v1.2.2 ExecPlan。
4. 建立仓库/包/符号地图。
5. 列出 Legacy 删除清单。
6. 冻结新 Domain Enum、ID、Hash、状态机。
7. 建立 Requirement → Goal → Code → Test 追踪。
8. 建立 EXT-BE-SKELETON/Runtime Candidate 状态记录。

## 禁止事项

- 不改变冻结架构；
- 不用 TODO 或 Mock 代替产品实现；
- 不修改 Provider 项目；
- 不修改无关功能。

## 验证

- 当前完整验证。
- Markdown/JSON 解析。
- Traceability 无未映射 MUST。
- 最低祖先校验。

## 证据交付

- ExecPlan
- Baseline report
- Repository/symbol map
- Traceability
- External dependency status

## 完成合同

- 基线真实通过；
- 后续 Goal 依赖明确；
- 无待实现方自行选择的 P0；
- Provider 项目无写操作；
- G01 可启动。

## 下一 Goal 交接

更新 Master ExecPlan，并将提交、测试、证据、阻断和权威输出交给下一 Goal。
