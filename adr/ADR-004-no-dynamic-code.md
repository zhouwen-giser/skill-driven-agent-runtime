# ADR-004：禁止执行 LLM 生成代码

- 状态：接受
- 日期：2026-07-11

## 背景

项目需要在快速复用开源能力的同时，保持 Goal、Skill、Workflow 和协议边界清晰。

## 决策

LLM 只生成受 JSON Schema 和白名单约束的 DSL。

## 理由

降低 RCE、审计和不可复现风险。

## 后果

- 所有实现与测试必须服从该边界。
- 如需变更，新增 superseding ADR，不直接覆盖历史。
